version: '3'
services:

    therabot:
        build: .
        environment:
            - DEBUG
            - SEARCHEVE_PORT
        restart: unless-stopped
        volumes:
            - ${SEARCHEVE_DATA_VOLUME:-data-volume}:/app/data

volumes:
    data-volume:
